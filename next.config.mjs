/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/cvpr2026",
    reactStrictMode: true,
    images: { unoptimized: true }
};

export default nextConfig;
